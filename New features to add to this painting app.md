New features to add to this painting application:

PNG as stamp

Improve texture
    - maybe try various texture algorithms
    - texture "editor" that allows users

Taper function is broken and does not work as intended, need to fix it so that it properly tapers the brush strokes based on speed and pressure. Currently it seems to be using max size and opacity for the supposed tapering, which means the last few stamps of the stroke are just large and opaque instead of tapering off.

There are still some issues with the visibility of the text in dropdown menus, and the text in the left sided "sliders", hard to read. Need to make sure that all text is clearly visible and legible, possibly by adjusting the colors or adding a background to the text.

Add a color picker tool that allows users to select colors from their painting or from a predefined palette. This would enhance the user experience by providing more flexibility in color selection.





