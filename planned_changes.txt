I need to add some of the more basic functions of a useful painting application:
reimplement copy/paste; selection with rectangle, ellipse, and lasso; deselect option; save (as bitmap file), save as..., and export (as layered image file such as photoshop format, procreate format), as well as other commonly found functions that are commonly used if I am missing obvious ones.
I also want to move the layer menu to a separate right side call out menu with a separate button on the top toolbar. We need to try to clean up the layer UI so that it allows user to move layers in the stack, grouping system with layers and subgrouping), control to group and ungroup, as well as duplicate groups and subgroups) and other manupulation. I want this area to be :drag and drop with touch or mouse click+drag.
I also want to implement ability to apply modification to a selected layer or layers, such as adjusting brightness, contrast, saturation, and applying filters like blur, sharpen, and color adjustments. Additionally, I want to add a history panel that allows users to undo and redo their actions, as well as a layer styles panel where users can apply effects like drop shadows, glows, and bevels to their layers.Furthermore, I want to include a text tool that allows users to add and edit text on their canvas, with options for font, size, color, and alignment.

Additional areas for improvement in the future include